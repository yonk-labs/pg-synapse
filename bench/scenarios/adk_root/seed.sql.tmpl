-- adk_root seed: Google ADK root_agent + tool parity scenario.
-- Creates a probe table for the agent to write into.

CREATE SCHEMA IF NOT EXISTS adk;

CREATE TABLE IF NOT EXISTS adk.probe (
    has_time BOOLEAN NOT NULL
);
